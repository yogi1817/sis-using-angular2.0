import { PropertyMetadata } from "./PropertyMetadata";
export interface SkipOptions {
    onSerialize: boolean;
    onDeserialize: boolean;
}
export declare class SkipMetadata extends PropertyMetadata {
    private _options;
    constructor(target: Function, key: string, options: SkipOptions);
    options: SkipOptions;
    isOnSerialize: boolean;
    isOnDeserialize: boolean;
}
